# Handoff inicial — Codex → Kimi/OpenClaw

## Estado verificado

- Repositorio canónico: <https://github.com/DraKenZaMaNosKe/audio-booster>
- Rama: `main`
- Baseline inicial: `133037d`
- El proyecto recibido no tenía historial Git ni repositorio GitHub coincidente.
- El APK debug original compiló y fue empaquetado antes de actualizar el toolchain.
- Dispositivos disponibles: Samsung SM-A155M y Huawei VNS-L53.

## Cambios realizados

- Auditoría funcional y de riesgos documentada.
- README corregido para reflejar capacidades reales.
- Gradle Wrapper 8.9 agregado.
- Matriz reproducible temporal: AGP 8.5.2, Kotlin 2.0.0 y Java 17; la subida a AGP 8.7.3 quedó pendiente por la descarga extremadamente lenta de `bundletool 1.17.1`.
- Conversión de volumen aislada en `VolumeMath` con tests unitarios.
- Roadmap, reglas de colaboración y prompt maestro para futuras apps agregados.

## Validación

- Build con configuración heredada: Kotlin compiló y `packageDebug` generó APK.
- Lint heredado: no concluyó y fue cancelado; no está aprobado.
- Build con toolchain actualizado: pendiente porque Google Maven dejó sin progreso la descarga de `bundletool 1.17.1`.
- QA funcional en teléfono: pendiente.

## Restricciones para continuar

1. No confundir una cadena global registrada con QA auditivo terminado: falta comparar y medir la salida.
2. El modo global depende del OEM porque la sesión 0 está obsoleta; conservar siempre el fallback local.
3. Repetir `./gradlew testDebugUnitTest assembleDebug lintDebug` cuando Google Maven responda.
4. La siguiente fase debe hacer honesta la UI y después integrar Media3 para boost real dentro de una sesión propia.
5. Mantener GitHub como fuente canónica y actualizar Drive como espejo de handoff.

## Actualización — MVP Media3

- Reproductor local Media3 1.9.2 conectado a `LoudnessEnhancer` mediante un audio session ID propio.
- Ganancia disponible: 0, +3 y +6 dB; +6 requiere confirmación de seguridad.
- Selección de pistas con Storage Access Framework, sin permiso amplio de almacenamiento.
- Eliminados controles engañosos y el foreground service sin reproducción real.
- Build y tests unitarios aprobados; instalación y arranque en Samsung SM-A155M aprobados.
- Modo global experimental añadido con `DynamicsProcessing` en sesión 0, limitador y foreground service; la cadena se comprobó en Samsung con logs y `AudioFlinger`.
- Metadatos externos añadidos mediante permiso opcional de acceso a notificaciones; no se captura audio.
- Arte y dial interactivo originales de Orbix integrados; ecualizador y pulido final siguen pendientes.
- Lint, autorización/QA de Spotify y medición acústica permanecen pendientes.
